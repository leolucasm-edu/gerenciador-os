<div class="row">
  <div class="col-lg-6">
    <%= link_to t('buttons.voltar'), servicos_path, :class=>"btn btn-sm btn-default" %>
  </div>
</div>
<br/>

<div class="row">
  <div class="col-lg-6">
    <div class="panel panel-default">
      <div class="panel-heading">
      <%= @operacao %>
      </div>
      <div class="panel-body">
        <div class="row">
          <div class="col-lg-12">
            <%= form_with(model: servico, local: true) do |form| %>
              <% if servico.errors.any? %>
                <div id="error_explanation">
                  <h2><%= pluralize(servico.errors.count, "error") %> prohibited this servico from being saved:</h2>
                  <ul>
                  <% servico.errors.full_messages.each do |message| %>
                    <li><%= message %></li>
                  <% end %>
                  </ul>
                </div>
              <% end %>

              <div class="form-group">
                <%= form.label Servico.human_attribute_name("descricao") %>
                <%= form.text_field :descricao, id: :servico_descricao, :class=>"form-control" %>
              </div>

              <div class="form-group">
                <%= form.label :preco_hora %>
                <%= form.text_field :preco_hora, id: :servico_preco_hora, :class=>"form-control currency" %>
              </div>

              <div class="actions">
                <button type="submit" class="btn btn-sm btn-primary"><%= t('buttons.gravar') %></button>
              </div>
            <% end %>
          </div>
        </div>
        <!-- /.row (nested) -->
      </div>
      <!-- /.panel-body -->
    </div>
    <!-- /.panel -->
  </div>
</div>
