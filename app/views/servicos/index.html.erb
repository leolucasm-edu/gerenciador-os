<div class="row">
    <div class="col-lg-12">
        <h1 class="page-header">Serviços</h1>
    </div>
    <!-- /.col-lg-12 -->
</div>
<!-- /.row -->

<p id="notice"><%= notice %></p>

<div class="row">
  <div class="col-lg-12">
    <div class="table-responsive">
        <table class="table table-striped table-hover">
            <thead>
            <tr>
              <th>Descricao</th>
              <th>Preco hora</th>
              <th colspan="3"></th>
            </tr>
          </thead>

          <tbody>
            <% @servicos.each do |servico| %>
              <tr>
                <td><%= servico.descricao %></td>
                <td><%= servico.preco_hora %></td>                
                <td style="width: 3%; text-align: center;">
                  <%= link_to servico, class:"btn btn-primary btn-circle" do %>
                    <i class="fa fa-eye"></i>
                  <% end %>
                </td>
                <td style="width: 3%; text-align: center;">
                  <%= link_to edit_servico_path(servico), class:"btn btn-success btn-circle" do %>
                    <i class="fa fa-edit"></i>
                  <% end %>
                </td>
                <td style="width: 3%; text-align: center;">
                  <%= link_to servico, method: :delete, data: { confirm: 'Are you sure?' }, class:"btn btn-danger btn-circle" do %>
                    <i class="fa fa-times"></i>
                  <% end %>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
      <br>

      <%= link_to 'New Servico', new_servico_path %>
  </div>
</div>
